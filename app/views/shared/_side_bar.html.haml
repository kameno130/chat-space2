.wrapper
  .chat-side
    .header
      .header-box
        %h2.header-box__user-name 
          = current_user.name
        %ul.header-box__user__menu
          .menu-icon
          = link_to "/groups/new", class: 'new-group'do
            =fa_icon 'pencil-square-o', class: 'icon'
          .menu-icon
          = link_to "/users/#{current_user.id}/edit", class: 'edit-user'do
            =fa_icon 'cog', class: 'icon'
    .groups
      - current_user.groups.each do |group|
        .group
          = link_to group_messages_path(group),class:'group__path' do
            %p.group__name 
              = group.name
            %p.group__message 
              メッセージはまだありません。